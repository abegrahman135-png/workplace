{
  "name": "prospect-organizer",
  "version": "0.1.0",
  "private": true,
  "description": "A local-only prospect organizer Chrome extension.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
