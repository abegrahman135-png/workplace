{
  "name": "prospect-finder",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Instagram prospect discovery — durable pipeline, local-only processing.",
  "scripts": {
    "test": "node --test tests/unit/*.test.js tests/integration/*.test.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "gen:fixture": "node tools/make_fixture.mjs",
    "gen:names": "node tools/make_names.mjs",
    "preview": "python3 -m http.server 8080 --bind 0.0.0.0"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
